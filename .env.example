# Copy to .env for `docker compose up` — same values the backend needs.
COGNODB_URI=bolt+s://<instance-id>.databases.cognodb.cloud
COGNODB_USER=cognodb
COGNODB_PASSWORD=
# CORS_ORIGINS=http://localhost:5173
